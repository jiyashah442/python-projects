"What is the right way to document modules in python?" | "Using print statements" | "Using Hashtag Comments" | "Using docstrings" | "Using inline comments after each line" | "Using docstrings"
"What best describes aliasing?" | "Creating a duplicate of a variable in memory" | "Assigning multiple names to the same value" | "Copying a value to a new location" | "Using a module without having to import it" | "Assigning multiple names to the same value"
"Which statement imports a module using alias?" | "import module_name as m" | "alias module_name as m" | "import module_name alias m" | "None of the above" | "import module_name as m"
"What is the main purpose of the if __name__ == \"__main__\": block in a Python program?" | "To define variables" | "To import modules into other programs" | "To run main program only when the file is run directly" | "To define the main function of a program" | "To run main program only when the file is run directly"
"What is the difference between a built-in module and a user-defined module in Python?" | "Built-in modules don't need to be imported while user-defined modules do" | "Built-in modules are only in Python and user-defined modules are in every coding language" | "Built-in modules come with Python and user-defined modules are created by the programmer" | "All of the above" | "Built-in modules come with Python and user-defined modules are created by the programmer"
"What is the proper way to test functions in Python?" | "Calling functions directly at the start of code" | "Writing test code inside the module" | "By using the if __name__ == '__main__': block" | "By commenting all test results in the main function" | "By using the if __name__ == '__main__': block"
"Why are functions used in computer science?" | "To reduce the need for variables" | "To avoid using loops and conditions" | "To organize code into reusable bloacks and improve readability" | "All of the above" | "To organize code into reusable bloacks and improve readability"
"How can you get the first element in the third row of the 2D list, matrix?" | "matrix[3][1]" | "matrix[2][0]" | "matrix[3+1]" | "matrix[0][-1]" | "matrix[2][0]"
"What will 'hello'[1:4] return?" | "'hell'" | "'ell'" | "'llo'" | "'olleh'" | "'ell'"
"Why is the .split() used on a string in Python?" | "To join both strings" | "To convert a string into a list based on a separator" | "To remove all whitespace from the string" | "None of the Above" | "To convert a string into a list based on a separator"
